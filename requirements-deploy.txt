# Production requirements for Render free tier (~200MB RAM at startup)
# pandas / numpy / scikit-learn / torch excluded — pure Python fallbacks used instead.
# sentence-transformers excluded — fuzzy + keyword matching covers 200+ merchants.

fastapi
uvicorn[standard]
pdfplumber
rapidfuzz
pydantic
pydantic[email]
groq
